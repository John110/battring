<p id="notice"><%= notice %></p>

<p>
  <strong>名前</strong>
  <%= @participant.name %>
</p>

<p>
  <strong>大会名</strong>
  <%= @create_matching.name %>
</p>

<%= link_to '参加した詳細に戻る', matching_path(@create_matching) %>|
<%= link_to '大会一覧に戻る', project_matchings_path(@create_matching.project_id, @create_matching.id) %>
